pipeline {
    agent any

    stages {
        stage('Build Docker image') {
            steps {
                sh 'docker build -t playwright-with-benefits -f ci/Dockerfile .'
            }
        }
    }
}