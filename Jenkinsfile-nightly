pipeline {
    agent any

    stages {
        stage('Build Docker image') {
            steps {
                sh 'docker build -t -f ci/Dockerfile playwright-with-benefits .'
            }
        }
    }
}