pipeline {
    agent any

    stages {
        stage('Build Docker&aws-cli image') {
            steps {
                sh 'docker build -t ubuntu-for-aws -f ci/aws/Dockerfile .'
            }
        }
    }
}